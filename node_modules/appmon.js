// HTML .....
<div class="container" id="app">
  <filter ></filter>  //allows user to enter filter info
  <conversations :data="conversations"></conversations>  //displays list of conversations that match filter indo
  <conversation :data="activeConv"></conversations>  //displays a single conversation
</div>

var mainApp = new Vue({
  el: "#app",
  data() {
    return {
      conversations: [{ convId :...}, ]
      activeConv :

    }
  },
  methods: {

    // these methods need ro be hooked up to relevant events that are rasied by the relevant components

    retrieveConversations: function () {
      // go get the data via REST api and whatever the filter info is
      //Axios looks like good lib to make REST calls
      this.conversations = ...

    }
    retrieveConversation: function () {
      // go get the data for a specifc conversation via REST api - called when the users selects an individual conversation
      this.activeConv = ...
    }

  }
});
